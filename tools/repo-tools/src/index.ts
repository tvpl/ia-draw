/**
 * Public surface of the repository audit tooling.
 *
 * Empty on purpose: T1 only scaffolds the workspace package; the extractors,
 * the capability-map checker and the audit CLI land in the tasks that follow.
 */
export {};
